<!-- current_user
@bookings = current_user.bookings
    @cars = current_user.cars
    @bookings.each.car -->

    Hi <% current_user %>

    your cars <% @cars.each do |car| %>
    <%= car.brand%>
    <%= car.model%>
    <% end %>

    your rental cars <% @bookings.each do |booking| %>
    <%= booking.car.brand%>
    <%= booking.car.model%>
    <% end %>

    your bookings <% @bookings.each do |booking| %>
    owner email <%= booking.user.email%>
    <br>
    owner name<%= booking.user.name%>
    <br>
    owner car <%= booking.car.model%> <%= booking.car.brand%>
    <% end %>
